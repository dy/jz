# Handoff 2026-08-22 — state + everything that remains

## State (final merge product @ e405de06; ready for user push)

- FINAL MATRIX: default 3639/3637/0/2; O0 3639/3637/0/2; O3
  3639/3637/0/2; WASI 3638/3636/0/2. Full test:wasm: 2892/2890/0/2.
  test262 language 3003/0/54 xfail (2507 neg-reject / 1538 neg-accept);
  builtins 853/0/86 xfail. kernel-parity 3/3 (33/33 byte-identical WAT at
  O0/O2/O3, all corpus rows). kernel-oracle 14/14 (605). Functional
  self-compile 21/21; ratchet 10/10; optimizer fixpoint 10/10.
  dist/jz.wasm 16,963.2 kB (the concurrent reject-experience wording slice
  accounts for the final increase from the test262 branch's 16,948.0 kB).
- Landed this campaign (see .work/phase-c-unification.md +
  .work/research.md for full trails): C1–C5b (plan-materialized unions at
  every join shape/level), C4a/C4b evidence-based host ABI (jz:hostabi;
  wrapVal stringify accident dead; rest covered) + closure-forwarding
  (closures' ctx.func.current plan-key gap fixed), neg-BigInt isBox
  sign-bit mask, printer .valueOf flat-key corruption (was BOTH red test
  trios) + registration guard (silent overwrite now throws), 5
  effect-erasing folds, typed-map fixes, effect-preserving equality folds,
  featgate pay-per-use joint dispatch, mapgrow grow-tier (nextCapIR
  2×→4×@8192), hashwall presize (−93.8% __hash_new_small), dyn-reach
  precision rungs 1–2 (per-schema + per-param union points-to),
  deletion-phase item 5 (−235 diagnostic lines) + three WALLs evidenced,
  ADR-0001 ratified (.work/adr-0001-bigint-representation.md).
- Accepted-wrong ledger: **zero explicit wrong-value assertions remain**.
  Ambiguous BOOL∪NUMBER local storage now rejects where identity would escape;
  full support still requires a future tagged Boolean carrier plan.
- 4 GiB self-compile gate: NOT closed. The proposed local-literal precision
  rung was measured and reverted at zero reach; next prerequisite is stable
  result/kind provenance for calls, members, elements and globals.

## Sentinel coverage — complete

- SENTINEL SLICE: the custodian draft @ f88caf3b was re-derived, corrected
  and validated as merge product @ 6125e2ae. RepresentationPlan now
  materializes unary-neg/unary-not/joint-binary may-BigInt return values;
  their wrappers take the generic tagged decode. The canonical joint-op
  set is shared from kind.js and the fresh raw-i64 producer is explicit.
  The legacy sentinel lane stayed as a fallback in this slice and was
  subsequently deleted by the single-authority completion below. Validation: syntax checks; data 147/147 (684); watr 37/37 (111);
  build including wat-strip 3/3 and dist/jz.wasm; kernel-oracle 14/14
  (619); kernel-parity 3/3 (33); full suite 3626/3624/0/2 (21,094);
  functional self-compile 21/21 (206); ratchet 10/10 (+0). The separately
  tracked warm performance pin remained red (1.059–1.081× versus 1.03);
  fresh-instance performance passed at 0.863×.

## coerceArg coverage — complete

- Merge product c7486727 makes binding readiness describe the carrier an
  expression actually emits, so watr's `uleb(BigInt(s), [])` call and its
  loop reassignment both receive plan-owned RAW→BOX actions. The assignment
  fallback is REJECT-only, preventing a plan action from double-boxing, and
  coerceArg's `sig.bigintBoxed` fallback is deleted. The full-suite
  instrumented pre-delete run recorded zero legacy fires; O0 WAT pins prove
  exactly one allocation at each edge. Validation: watr 37/37 (113), data
  147/147, pointers 35/35, statements 202/202, closures 114/114, build and
  wat-strip 3/3, kernel-oracle 14/14, kernel-parity 3/3, full suite
  3626/3624/0/2 (21,096), functional self-compile 21/21 and ratchet 10/10.

## parameter second phase — complete

- Merge product b00e68c3 removes all four RepresentationPlan reads of
  `bigintBoxed`. Boundary solving keeps its coarse pre-body current; after
  body materialization, `representationParamRep(..., false)` reads the
  ready parameter's target as its active entry carrier without mutating the
  published boundary. Reassigned Number/BigInt params therefore normalize
  through one body-plan verdict. Validation: data 147/147, watr 37/37,
  closures 114/114, inference 142/142, pointers 35/35, statements 202/202,
  session-reentrancy 20/20, build/wat-strip 3/3, kernel-oracle 14/14,
  kernel-parity 3/3 and full suite 3626/3624/0/2 (21,096).

## one representation authority — complete

- Commits 443dfc60 + 20fe3b22 delete `bigintBoxed` end to end: ValueRep
  field and clone row, analyze sink/capture walk, narrow verdict/stamps,
  coerce/return/assignment/ternary consumers and stale diagnostics. Generic
  stored-value edges now ask RepresentationPlan directly; nested literal
  BigInt origins retain an explicit RAW producer fact. The final census
  result sentinel ABI (`jz:i64exp.s`, layout tables, interop decoder and
  hand-built wrapper) is deleted; bare, unary and joint results all use the
  generic tagged decode. Net change for this completion: −755 lines across
  the two source commits; dist/jz.wasm 17,115.3→17,082.8 kB. Validation:
  data 147/147, watr 37/37, types 178/178, ABI 5/5, interop 18/18,
  dyn-keys 69/69, passes 9/9, session 20/20, build/wat-strip 3/3,
  kernel-oracle 14/14, kernel-parity 3/3, full suite 3626/3624/0/2
  (21,096), functional self-compile 21/21 and ratchet 10/10 (+0).

## Precision rung 3 — measured wall, no source landed

- A fail-closed local def-site solver was implemented and pinned on literal,
  reassignment-union and opaque-call shapes, then run against the current
  O3 self graph. Fresh attribution falsified the handoff premise: 4,923
  unresolved local-receiver attempts, **0 resolved**, 4,923 ALL. Every sampled
  live def was a call/member result, array element, global alias or missing
  initializer—not a literal/known-sid RHS. `dynPointsTo` remained `ALL`; the
  compiler artifact grew 3,218 bytes (17,512,988→17,516,206) and native build
  time moved 306.6→325.9 s. The synthetic slice was reverted completely;
  running the instrumented 4 GiB goal gate would measure no codegen change.
  Closing this wall requires scope-stable result/kind provenance for calls,
  elements and globals (or indexed HIR), not another local literal walker.

## Date flat-key corruption — complete

- Commit 204cf9cc routes every Date-only flat emitter through the same
  PTR.OBJECT + dateSid discriminator as `.valueOf`. Erased/runtime Dates
  call the type-qualified emitter; in-wasm non-Dates throw TypeError; host
  Date objects take `__ext_call`. Receiver/args evaluate once in JS order,
  including ignored and spread args; nullish receivers throw before args.
  Argument-taking methods stage values so omitted and explicit undefined
  remain distinct; dynamic setter spreads loud-reject. `.getTime()` no longer
  reads array element 0, and the KNOWN-WRONG pin is gone. Validation: date
  37/37 (141), data 147/147, array-methods 144/145+1 skip, objects 134/134,
  parser-bugs 23/23, webglobals 26/26, build/wat-strip 3/3,
  kernel-oracle 14/14, kernel-parity 3/3, full suite 3630/3628/0/2
  (21,125), functional self-compile 21/21 and ratchet 10/10 (+0).

## Conditional writes and C5b adjacent gaps — complete

- Commit b9dd87fe gives `&&=`/`||=`/`??=` real binding-write edges: only the
  taken RHS normalizes, so nullish-assigned BigInt is tagged without changing
  untaken values or effects. Join semantics now preserve nested non-BigInt
  arm kinds instead of injecting a fictitious BOOL veto; covered bare params
  use their closed observed kinds only at join arms (the broader boundary
  change was falsified by watr parity and not kept). Boundary wrappers also
  distinguish a raw-specialized callee result from a stale coarse tagged
  result after O2/O3 transformation. All three C5b-adjacent shapes are pinned
  at O0/O2/O3. Validation: data 149/149 (723), watr 37/37 (113), pointers
  35/35, statements 202/202, closures 114/114, inference 142/142,
  array-methods 144/145+1 skip, dyn-keys 69/69, build/wat-strip 3/3,
  kernel-oracle 14/14, kernel-parity 3/3, full suite 3632/3630/0/2
  (21,164), functional self-compile 21/21 and ratchet 10/10 (+0).

## Accepted-wrong sweep — complete

- Commit b9ce0c62 removes the last explicit wrong-value assertions while
  deleting 435 net lines. Dynamic ToNumber no longer guesses BigInt from
  subnormal magnitude: plan-tagged BigInt takes PTR.BIGINT, every non-NaN f64
  remains Number. Conditional spreads always use HASH insertion, preserving
  absent versus present-undefined; Object.assign marks dynamic target writes,
  and the obsolete condAbsent side table/specialized enumerators are deleted.
  Ambiguous BOOL∪NUMBER locals correct-reject when a stored value reaches an
  identity-sensitive use; truthiness-only and captured-shadow paths remain
  accepted. dist/jz.wasm shrank 17,107.4→16,966.0 kB. Validation: conditional
  spread 21/21 (106), data 149/149 (729), number 53/53, objects 134/134,
  spread 40/41+1 skip, watr 37/37, kernel-oracle 14/14 (605), build/wat-strip
  3/3, kernel-parity 3/3, full suite 3632/3630/0/2 (21,156), functional
  self-compile 21/21 and ratchet 10/10 (+0).

## Full test:wasm loop-hoist trio — complete

- Commit 5104b467 fixes all three failures. Fresh-process reproduction
  falsified the handoff's order/state-leak diagnosis: the two perf rows failed
  only on the kernel BYTES path (compileWat printed `local.get undefined`),
  and the ablation row never hoisted. In self-compiled execution, a Map and
  later arrays captured by walkAst callbacks lost the chosen local mapping;
  spread-form `splice(...preheaders)` also failed to insert reliably. The pass
  now scans/replaces by direct recursion, keeps the tiny mapping in parallel
  arrays, and inserts one node at a time. Native decisions/order are unchanged.
  Validation: kernel perf 55/55, kernel wat-invariants 24/24, full test:wasm
  2885/2883/0/2 (13,813), build/wat-strip 3/3, native full suite
  3632/3630/0/2 (21,156), kernel-oracle 14/14, kernel-parity 3/3,
  functional self-compile 21/21 and ratchet 10/10 (+0).

## Emitter self-demands / derived autoload narrowing — complete

- Commit 1b4c28ae removes the derived-only catch-all. Synthesized `in`, `str`,
  sidecar/external dispatch and typed-buffer fallbacks now include their own
  string/array/collection dependencies at the emission seam. A freshness pin
  requires a generated-only row (`getDate`) to equal its derived owners rather
  than inherit object/array/string/collection. The historical −737 kB estimate
  was stale after intervening module changes; current dist/jz.wasm shrank
  16,970.1→16,918.1 kB (−52.0 kB). Validation: full suite
  3632/3630/0/2 (21,156), self-compile-includes 3/3 (5), feature-gating 34/34,
  array-methods 144/145+1 skip, objects 134/134, buffer 58/58,
  build/wat-strip 3/3, kernel-oracle 14/14, kernel-parity 3/3,
  functional self-compile 21/21 and ratchet 10/10 (+0).

## Opaque `.length` + `Array.from` — complete

- Source commit e867c3af replaces unresolved `.length`'s i32/header shortcut
  with ordinary f64 property Get. ARRAY/TYPED/STRING retain their direct paths;
  OBJECT/HASH use dynamic schema/hash Get; PTR.EXTERNAL uses `__ext_prop`;
  nullish access throws one shared branded TypeError; optional chaining still
  short-circuits. Host getters execute exactly once. The shared
  `__length_num` helper keeps numeric consumers compact without claiming the
  raw property value is numeric.
- `Array.from` keeps the ARRAY memory-copy fast path and typed-width reads, but
  its array-like path now evaluates source and mapper arguments in JS order,
  validates callable map functions before reading `length`, performs one
  length Get + ToLength, and performs a fresh indexed Get per element. The
  static unroll/cap shortcut is deleted. Plain host array-likes work; unsupported
  iterator-protocol sources reject after their single observable iterator Get.
  wasm32-overflowing lengths and unsupported BigInt lengths reject loudly.
- The same re-audit exposed two source-order bugs beneath the retired shortcut:
  duplicate object initializers were erased, and computed keys were moved after
  static/spread properties. Prepare now stages duplicate values left-to-right
  and lowers computed/static/spread definitions in source order. External
  property getters cache the value so one Get is one host access.
- The first correct implementation grew dist/jz.wasm 16,918.1→17,278.2 kB
  (+360.1 kB) by inlining null guards/coercions at every unresolved read. It was
  not landed. Shared runtime materialization reduced the merged semantic slice
  to 16,956.2 kB; the warm-dispatch repair below brings it to 16,949.1 kB.
- Validation on the rebuilt source commit: build + wat-strip 3/3; native full
  suite 3637/3635/0/2 (21,201); full test:wasm 2890/2888/0/2 (13,842);
  functional self-compile 21/21 (206); kernel-parity 3/3 (33); kernel-oracle
  14/14 (605); ratchet 10/10 (+0). The already-standing warm pin remains red
  at 1.066×/1.091×/1.094× versus 1.03; fresh-instance passes at 0.870×.

## Warm-pin bisect + opaque dispatch recovery — complete

- The local timing environment was not quiet: four orphaned `strbuild` bench
  binaries had each consumed one full core continuously for 8–11 days. They
  were terminated by PID. Even afterwards the stopwatch ratio remained
  thermally/JIT-sensitive: the identical Aug-16 `6adde429` artifact measured
  1.025× warm, then 1.108–1.161× later with no source change. The strict cap
  was not loosened and no commit was classified from an unpaired stopwatch.
- Same-process, order-alternated artifact A/B isolated the source signal. The
  campaign baseline `11588771` was 0.987× and pre-opaque `b028ea65` was 0.994×
  versus the Aug-16 artifact: the representation campaign had not regressed
  warm Wasm. The merged opaque tree was 1.041× versus `b028ea65`, localizing
  the real regression entirely to the new length path.
- Root cause had two general parts. Numeric use called
  `__length_num → __length → __to_num`, repeating dispatch on ARRAY hot paths;
  and `.length` read `rep ? rep.val : valTypeOf`, so the mere existence of an
  unrelated durable rep hid higher-priority flow facts. Thus the canonical
  compiler shape `if (!Array.isArray(node)) return; node.length` stayed
  polymorphic thousands of times.
- Source commit 8616e2b2 restores lookup priority via `lookupValType`, splits
  raw `__length.value` from numeric `__length` while generating both from one
  source, and outlines ToNumber to the ordinary-property tail. ARRAY/TYPED/
  STRING never pay it. Immediately paired against `b028ea65`, the repaired
  artifact is 1.011× (merged opaque: 1.041×), recovering about three percentage
  points while preserving object/host/nullish semantics. dist/jz.wasm shrinks
  16,956.2→16,949.1 kB.
- Rebuilt validation: native full suite 3638/3636/0/2; test:wasm
  2891/2889/0/2; self-compile 21/21; parity 3/3; oracle 14/14; ratchet 10/10.
  Fresh-instance remains green at 0.838–0.847×. The V8-referenced warm stopwatch
  still reads 1.037–1.067× on this active GUI machine; direct artifact A/B is
  the authoritative attribution result, not grounds to relax the 1.03 cap.

## test262 language + builtins legs — complete

- The first language run exposed two in-scope class-grammar failures introduced
  by duplicate-key effect preservation: staging one synthetic arrow parameter
  per property tied legal object size to MAX_CLOSURE_ARITY=8. Commit ec4e1b22
  stages all initializers in one uniquely-keyed object argument instead. The
  12-field fixtures and ordinary duplicate source-order pins now both pass.
- The negative-parse ceiling had silently moved 1889→2470 at 59c23e90. The DCE
  itself is sound; the runner replaced test262's `$DONOTEVALUATE()` sentinel
  with `return`, making invalid syntax a dead tail that the new prepare pass
  correctly removed. Compile-only negative tests now replace the sentinel with
  inert `0`, retaining invalid syntax for rejection. Negative accepts improve
  to 1538 and the committed ceiling ratchets downward to that value; it was not
  raised. subscript 10.7.0/10.7.1 differ by exactly one row (1537/1538).
- `Array.from/elements-added-after.js` now passes through live indexed Get, so
  its stale expected-fail entry is deleted. Final language result: 3003 pass,
  0 in-scope fail, 54 xfail, 2507 negative rejects, 1538 negative accepts.
  Builtins: 853 pass, 0 fail, 86 xfail. Baseline floors move 3000→3003 and
  852→853.
- Rebuilt companion gates: native 3638/3636/0/2; test:wasm
  2891/2889/0/2; self 21/21; parity 3/3; oracle 14/14; ratchet 10/10.
  dist/jz.wasm is 16,948.0 kB.

## Final-matrix O0 catch ordering — complete

- The first full matrix found one O0-only failure: catching nullish `.length`
  and then reading `e.name` could return false or walk an uninitialized schema
  table. The shared TypeError helper minted its branded schema lazily during
  stdlib pull, after O0 catch/property planning had frozen schema metadata.
- Commit 8954dac2 keeps allocation and strings shared, but registers the
  TypeError schema at both emission seams (`emitLengthAccess` and Array.from's
  direct helper call). The minimal catch returns TypeError at O0/O1/O2/O3;
  the complete O0 leg is 3639/3637/0/2.
- The first WASI continuation exposed seven JS-host-only assertions that had
  never been scoped per `test/_matrix.js`'s contract: host object/Date dispatch,
  EXTERNAL-import structure, and callable `run` return values (WASI reserves
  `run` as a void command entry). Commit d1a6276b guards only those assertions;
  internal opaque-length checks remain live. Full WASI: 3638/3636/0/2.

## Final certification — complete

- `npm run build`: wat-strip parity 3/3; dist/jz.wasm 16,963.2 kB.
- `npm run test:matrix`: all four legs zero-fail at the totals in State.
- Full test:wasm 2892/2890/0/2; functional self-compile 21/21; parity 3/3;
  oracle 14/14; ratchet 10/10; optimizer fixpoint 10/10.
- test262 language and builtins both pass their upward/downward committed locks.
- Warm stopwatch remains machine-sensitive and red locally at
  1.039×/1.057×/1.052× versus 1.03; fresh passes at 0.851×. The cap was not
  loosened. Same-process artifact A/B already isolated and repaired the real
  opaque-length regression (1.041×→1.011× versus pre-opaque).
- Dormant jz×jz goal probe, exact documented ABI
  `default(code,0,0,modules,0)`: 158 modules, expected `unreachable` after
  8.596 s at exactly 4,294,967,296 memory bytes (heap i32 = -80). The blocker
  remains pure wasm32 volume; no new corruption signature appeared.

## Superseded continuation — v1 architecture campaign

The user pushed the prior certification (`302f43dd` is the new campaign base)
and then reopened work against all seven audit findings. The authoritative
continuation is `.work/v1-architecture-campaign.md`.

Slice 1 on `fix/v1-value-provenance` closes the typed value/storage wrong-value
class and open Error-message coercion while preserving the existing matrix,
self-compile parity/oracle, ratchet, test262, and golden-size gates. It adds one
cycle-free typed constructor/result authority and producer-side tagging for
mixed indirect BigInt results. Slice 2 closes both Shape #6 residuals (++/--
and closure-local storage reads), including the real watr HANDLER dispatch.
Slice 3 reconciles the finite semantics dialect, corrects the public bigint
carrier types, adds declarations for all package subpaths, and explicitly
defers the unversioned raw Wasm ABI rather than freezing it by accident.
Slice 4 unifies typed ctor expression provenance, publishes sparse per-body
TypedStoragePlans, routes typed emitters through them, and turns a missing
active BigInt plan into an invariant failure. Slice 5 validates early errors
before jzify in both native and kernel compilers, reduces accepted-invalid
negative parses 1538→187, and exact-set gates the classified residual ledger.
`dist/jz.wasm` is about 17,259 kB; full 162-module jz×jz still traps at exactly
2^32 bytes. The 187 remain a v1 blocker; then come memory/performance/native. The agent still must not push.

## Process rules that kept this sound (binding for any resumer)

- Serial merges only; every branch validates its MERGE PRODUCT (merge
  main into branch, rebuild, battery) before merging back; bar = ZERO
  fails at the current suite total. Build before suites (stale dist =
  false fails). pwd-verify before every git/build/test command.
- Git safety: no repo-wide commands (stash/checkout/reset/clean/switch/
  restore), stage named files only, user is sole author (no
  Co-Authored-By / Generated-with), never push/pull — the user pushes.
- .work/todo-original.md in the main worktree belongs to the user.
- Agents: never park on background-wake promises (they die); hold turns
  with chained bounded-wait loops (`for i in $(seq 1 27); do <check> &&
  break; sleep 15; done`, immediately followed by the next). Zombie
  recovery that works: TaskStop, then SendMessage (resumes from
  transcript at the exact stall point). Dead-man watch: file-driven
  watchlist + transcript-mtime staleness + main-HEAD moves
  (scratchpad/deadman10.sh pattern).
- Bench/demo sources are fixed specimens: fix the engine, never the
  input. Wrong-value classes outrank performance work. KNOWN-WRONG pins
  are trail markers with named flip conditions — zero allowed at release
  on semantic paths.
