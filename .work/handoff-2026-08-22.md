# Handoff 2026-08-22 — state + everything that remains

## State (parameter second-phase merge product @ b00e68c3; main follows by serial merge)

- FULL SUITE: 3626 total / 3624 pass / 0 fail / 2 skip. kernel-parity 3/3
  (33/33 byte-identical WAT at O0/O2/O3, all corpus rows). kernel-oracle
  14/14 (619). Single worktree, no loose branches (agent/opaque-length =
  intentional custodian archive @ 7677eb2b).
- Landed this campaign (see .work/phase-c-unification.md +
  .work/research.md for full trails): C1–C5b (plan-materialized unions at
  every join shape/level), C4a/C4b evidence-based host ABI (jz:hostabi;
  wrapVal stringify accident dead; rest covered) + closure-forwarding
  (closures' ctx.func.current plan-key gap fixed), neg-BigInt isBox
  sign-bit mask, printer .valueOf flat-key corruption (was BOTH red test
  trios) + registration guard (silent overwrite now throws), 5
  effect-erasing folds, typed-map fixes, effect-preserving equality folds,
  featgate pay-per-use joint dispatch, mapgrow grow-tier (nextCapIR
  2×→4×@8192), hashwall presize (−93.8% __hash_new_small), dyn-reach
  precision rungs 1–2 (per-schema + per-param union points-to),
  deletion-phase item 5 (−235 diagnostic lines) + three WALLs evidenced,
  ADR-0001 ratified (.work/adr-0001-bigint-representation.md).
- Wrong-value ledger: ONLY `.getTime()` KNOWN-WRONG (test/date.js, flip
  condition named). Everything else fixed or loud-rejects.
- 4 GiB self-compile gate: NOT closed. Dormant-config jz×jz traps at
  exactly 4 GiB in ~8s. Diffuse sum; dominant single terms killed 16×.
  Remaining lever = precision rung 3 (below).

## Sentinel coverage — complete

- SENTINEL SLICE: the custodian draft @ f88caf3b was re-derived, corrected
  and validated as merge product @ 6125e2ae. RepresentationPlan now
  materializes unary-neg/unary-not/joint-binary may-BigInt return values;
  their wrappers take the generic tagged decode. The canonical joint-op
  set is shared from kind.js and the fresh raw-i64 producer is explicit.
  The legacy sentinel lane deliberately remains as a dead fallback until
  dedicated instrumentation proves zero fires; it was not deleted in this
  slice. Validation: syntax checks; data 147/147 (684); watr 37/37 (111);
  build including wat-strip 3/3 and dist/jz.wasm; kernel-oracle 14/14
  (619); kernel-parity 3/3 (33); full suite 3626/3624/0/2 (21,094);
  functional self-compile 21/21 (206); ratchet 10/10 (+0). The separately
  tracked warm performance pin remained red (1.059–1.081× versus 1.03);
  fresh-instance performance passed at 0.863×.

## coerceArg coverage — complete

- Merge product c7486727 makes binding readiness describe the carrier an
  expression actually emits, so watr's `uleb(BigInt(s), [])` call and its
  loop reassignment both receive plan-owned RAW→BOX actions. The assignment
  fallback is REJECT-only, preventing a plan action from double-boxing, and
  coerceArg's `sig.bigintBoxed` fallback is deleted. The full-suite
  instrumented pre-delete run recorded zero legacy fires; O0 WAT pins prove
  exactly one allocation at each edge. Validation: watr 37/37 (113), data
  147/147, pointers 35/35, statements 202/202, closures 114/114, build and
  wat-strip 3/3, kernel-oracle 14/14, kernel-parity 3/3, full suite
  3626/3624/0/2 (21,096), functional self-compile 21/21 and ratchet 10/10.

## parameter second phase — complete

- Merge product b00e68c3 removes all four RepresentationPlan reads of
  `bigintBoxed`. Boundary solving keeps its coarse pre-body current; after
  body materialization, `representationParamRep(..., false)` reads the
  ready parameter's target as its active entry carrier without mutating the
  published boundary. Reassigned Number/BigInt params therefore normalize
  through one body-plan verdict. Validation: data 147/147, watr 37/37,
  closures 114/114, inference 142/142, pointers 35/35, statements 202/202,
  session-reentrancy 20/20, build/wat-strip 3/3, kernel-oracle 14/14,
  kernel-parity 3/3 and full suite 3626/3624/0/2 (21,096).

## Remaining queue, in order

1. RESUME DELETION items 1–4 (per-item instrumentation protocol is
   documented and proven; item 6 sweep after).
2. Precision rung 3 — LOCAL-VARIABLE provenance for dynPointsTo: 72% of
   residual 'ALL' fallbacks (16,513 sites; m5_parse `cur` etc.). Extend
   program-facts.js resolveParamUnion's approach to local def-sites
   (let/const initializers with literal/known-sid RHS; reassignment joins;
   fail-closed). Then re-measure the dormant goal-gate
   (JZ_HELPER_SITES=dyn_set). This is the biggest remaining 4 GiB lever.
   Residual after: 17% param-ALL, 11% dispatch chains (ctx singleton —
   hard, documented in the union-points-to report).
3. `.getTime()` flat-key gap: KNOWN-WRONG pinned in test/date.js. Same
   dateSid-aux discriminator as the landed .valueOf fix, but reaches
   tryGenericEmitter (three call sites, obj not always materialized) —
   needs its own wiring.
4. test:wasm order-dependent trio: 3 loop-hoisting tests fail "Unknown
   local undefined" ONLY in full-suite order under JZ_TEST_TARGET=jz.wasm
   — pre-existing on 114a373b (verified), kernel session-state fragility.
   Root-cause slice.
5. C5b's two documented adjacent gaps (phase-c doc §C5b): (a) bare
   open-param arm can't materialize through any join op (C1
   mixed-entry-param territory); (b) semanticOf's !mayCarryBigint
   fallback trips the BOOL-veto for one nullish-vs-typed nested-ternary
   '??' shape.
6. Site-by-site self-demand campaign: re-earn the −737kB — each
   hand-built-IR emitter declares its own includeFor* demand
   (buildArrayWithSpreads = the done template; object.js 'in' node,
   'str' sites, 2 stdlib-registration cases), then narrow derived-only
   CATCH_ALL autoload rows suite-gated (src/autoload.js).
7. Opaque-length redesign: custodian trail on agent/opaque-length
    (7677eb2b, NOT landable — marshalled host .length returned undefined).
    Bars: JS property access stays f64 __length/dynamic-Get; Array.from
    gets ToLength + indexed-Get; external demand at the length seam.
8. Array.from effect-order/cap gate: JS-observable evaluation order or
    reject; ARRAY_LIKE_STATIC_CAP hole.
9. WALL decl-storage three-site fix (two prior reverts — highest-risk
    item, do last among code slices).
10. Warm-pin bisect (quiet machine required; 1.04-1.06× vs 1.03 cap).
11. test262 leg (after the accepted-wrong ledger is empty).
12. FINAL: full battery (build + kernel-oracle + kernel-parity + full
    suite + goal-gate) → USER PUSHES → CI bench matrix regenerates the
    perf evidence (currently 270+ source commits stale). Push is the
    user's action, never the agent's.

## Process rules that kept this sound (binding for any resumer)

- Serial merges only; every branch validates its MERGE PRODUCT (merge
  main into branch, rebuild, battery) before merging back; bar = ZERO
  fails at the current suite total. Build before suites (stale dist =
  false fails). pwd-verify before every git/build/test command.
- Git safety: no repo-wide commands (stash/checkout/reset/clean/switch/
  restore), stage named files only, user is sole author (no
  Co-Authored-By / Generated-with), never push/pull — the user pushes.
- .work/todo-original.md in the main worktree belongs to the user.
- Agents: never park on background-wake promises (they die); hold turns
  with chained bounded-wait loops (`for i in $(seq 1 27); do <check> &&
  break; sleep 15; done`, immediately followed by the next). Zombie
  recovery that works: TaskStop, then SendMessage (resumes from
  transcript at the exact stall point). Dead-man watch: file-driven
  watchlist + transcript-mtime staleness + main-HEAD moves
  (scratchpad/deadman10.sh pattern).
- Bench/demo sources are fixed specimens: fix the engine, never the
  input. Wrong-value classes outrank performance work. KNOWN-WRONG pins
  are trail markers with named flip conditions — zero allowed at release
  on semantic paths.
