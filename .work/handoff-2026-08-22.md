# Handoff 2026-08-22 — state + everything that remains

## State (self-compiled loop-hoist merge product @ 5104b467; main follows by serial merge)

- FULL SUITE: 3632 total / 3630 pass / 0 fail / 2 skip. kernel-parity 3/3
  (33/33 byte-identical WAT at O0/O2/O3, all corpus rows). kernel-oracle
  14/14 (619). Single worktree, no loose branches (agent/opaque-length =
  intentional custodian archive @ 7677eb2b).
- Landed this campaign (see .work/phase-c-unification.md +
  .work/research.md for full trails): C1–C5b (plan-materialized unions at
  every join shape/level), C4a/C4b evidence-based host ABI (jz:hostabi;
  wrapVal stringify accident dead; rest covered) + closure-forwarding
  (closures' ctx.func.current plan-key gap fixed), neg-BigInt isBox
  sign-bit mask, printer .valueOf flat-key corruption (was BOTH red test
  trios) + registration guard (silent overwrite now throws), 5
  effect-erasing folds, typed-map fixes, effect-preserving equality folds,
  featgate pay-per-use joint dispatch, mapgrow grow-tier (nextCapIR
  2×→4×@8192), hashwall presize (−93.8% __hash_new_small), dyn-reach
  precision rungs 1–2 (per-schema + per-param union points-to),
  deletion-phase item 5 (−235 diagnostic lines) + three WALLs evidenced,
  ADR-0001 ratified (.work/adr-0001-bigint-representation.md).
- Accepted-wrong ledger: **zero explicit wrong-value assertions remain**.
  Ambiguous BOOL∪NUMBER local storage now rejects where identity would escape;
  full support still requires a future tagged Boolean carrier plan.
- 4 GiB self-compile gate: NOT closed. The proposed local-literal precision
  rung was measured and reverted at zero reach; next prerequisite is stable
  result/kind provenance for calls, members, elements and globals.

## Sentinel coverage — complete

- SENTINEL SLICE: the custodian draft @ f88caf3b was re-derived, corrected
  and validated as merge product @ 6125e2ae. RepresentationPlan now
  materializes unary-neg/unary-not/joint-binary may-BigInt return values;
  their wrappers take the generic tagged decode. The canonical joint-op
  set is shared from kind.js and the fresh raw-i64 producer is explicit.
  The legacy sentinel lane stayed as a fallback in this slice and was
  subsequently deleted by the single-authority completion below. Validation: syntax checks; data 147/147 (684); watr 37/37 (111);
  build including wat-strip 3/3 and dist/jz.wasm; kernel-oracle 14/14
  (619); kernel-parity 3/3 (33); full suite 3626/3624/0/2 (21,094);
  functional self-compile 21/21 (206); ratchet 10/10 (+0). The separately
  tracked warm performance pin remained red (1.059–1.081× versus 1.03);
  fresh-instance performance passed at 0.863×.

## coerceArg coverage — complete

- Merge product c7486727 makes binding readiness describe the carrier an
  expression actually emits, so watr's `uleb(BigInt(s), [])` call and its
  loop reassignment both receive plan-owned RAW→BOX actions. The assignment
  fallback is REJECT-only, preventing a plan action from double-boxing, and
  coerceArg's `sig.bigintBoxed` fallback is deleted. The full-suite
  instrumented pre-delete run recorded zero legacy fires; O0 WAT pins prove
  exactly one allocation at each edge. Validation: watr 37/37 (113), data
  147/147, pointers 35/35, statements 202/202, closures 114/114, build and
  wat-strip 3/3, kernel-oracle 14/14, kernel-parity 3/3, full suite
  3626/3624/0/2 (21,096), functional self-compile 21/21 and ratchet 10/10.

## parameter second phase — complete

- Merge product b00e68c3 removes all four RepresentationPlan reads of
  `bigintBoxed`. Boundary solving keeps its coarse pre-body current; after
  body materialization, `representationParamRep(..., false)` reads the
  ready parameter's target as its active entry carrier without mutating the
  published boundary. Reassigned Number/BigInt params therefore normalize
  through one body-plan verdict. Validation: data 147/147, watr 37/37,
  closures 114/114, inference 142/142, pointers 35/35, statements 202/202,
  session-reentrancy 20/20, build/wat-strip 3/3, kernel-oracle 14/14,
  kernel-parity 3/3 and full suite 3626/3624/0/2 (21,096).

## one representation authority — complete

- Commits 443dfc60 + 20fe3b22 delete `bigintBoxed` end to end: ValueRep
  field and clone row, analyze sink/capture walk, narrow verdict/stamps,
  coerce/return/assignment/ternary consumers and stale diagnostics. Generic
  stored-value edges now ask RepresentationPlan directly; nested literal
  BigInt origins retain an explicit RAW producer fact. The final census
  result sentinel ABI (`jz:i64exp.s`, layout tables, interop decoder and
  hand-built wrapper) is deleted; bare, unary and joint results all use the
  generic tagged decode. Net change for this completion: −755 lines across
  the two source commits; dist/jz.wasm 17,115.3→17,082.8 kB. Validation:
  data 147/147, watr 37/37, types 178/178, ABI 5/5, interop 18/18,
  dyn-keys 69/69, passes 9/9, session 20/20, build/wat-strip 3/3,
  kernel-oracle 14/14, kernel-parity 3/3, full suite 3626/3624/0/2
  (21,096), functional self-compile 21/21 and ratchet 10/10 (+0).

## Precision rung 3 — measured wall, no source landed

- A fail-closed local def-site solver was implemented and pinned on literal,
  reassignment-union and opaque-call shapes, then run against the current
  O3 self graph. Fresh attribution falsified the handoff premise: 4,923
  unresolved local-receiver attempts, **0 resolved**, 4,923 ALL. Every sampled
  live def was a call/member result, array element, global alias or missing
  initializer—not a literal/known-sid RHS. `dynPointsTo` remained `ALL`; the
  compiler artifact grew 3,218 bytes (17,512,988→17,516,206) and native build
  time moved 306.6→325.9 s. The synthetic slice was reverted completely;
  running the instrumented 4 GiB goal gate would measure no codegen change.
  Closing this wall requires scope-stable result/kind provenance for calls,
  elements and globals (or indexed HIR), not another local literal walker.

## Date flat-key corruption — complete

- Commit 204cf9cc routes every Date-only flat emitter through the same
  PTR.OBJECT + dateSid discriminator as `.valueOf`. Erased/runtime Dates
  call the type-qualified emitter; in-wasm non-Dates throw TypeError; host
  Date objects take `__ext_call`. Receiver/args evaluate once in JS order,
  including ignored and spread args; nullish receivers throw before args.
  Argument-taking methods stage values so omitted and explicit undefined
  remain distinct; dynamic setter spreads loud-reject. `.getTime()` no longer
  reads array element 0, and the KNOWN-WRONG pin is gone. Validation: date
  37/37 (141), data 147/147, array-methods 144/145+1 skip, objects 134/134,
  parser-bugs 23/23, webglobals 26/26, build/wat-strip 3/3,
  kernel-oracle 14/14, kernel-parity 3/3, full suite 3630/3628/0/2
  (21,125), functional self-compile 21/21 and ratchet 10/10 (+0).

## Conditional writes and C5b adjacent gaps — complete

- Commit b9dd87fe gives `&&=`/`||=`/`??=` real binding-write edges: only the
  taken RHS normalizes, so nullish-assigned BigInt is tagged without changing
  untaken values or effects. Join semantics now preserve nested non-BigInt
  arm kinds instead of injecting a fictitious BOOL veto; covered bare params
  use their closed observed kinds only at join arms (the broader boundary
  change was falsified by watr parity and not kept). Boundary wrappers also
  distinguish a raw-specialized callee result from a stale coarse tagged
  result after O2/O3 transformation. All three C5b-adjacent shapes are pinned
  at O0/O2/O3. Validation: data 149/149 (723), watr 37/37 (113), pointers
  35/35, statements 202/202, closures 114/114, inference 142/142,
  array-methods 144/145+1 skip, dyn-keys 69/69, build/wat-strip 3/3,
  kernel-oracle 14/14, kernel-parity 3/3, full suite 3632/3630/0/2
  (21,164), functional self-compile 21/21 and ratchet 10/10 (+0).

## Accepted-wrong sweep — complete

- Commit b9ce0c62 removes the last explicit wrong-value assertions while
  deleting 435 net lines. Dynamic ToNumber no longer guesses BigInt from
  subnormal magnitude: plan-tagged BigInt takes PTR.BIGINT, every non-NaN f64
  remains Number. Conditional spreads always use HASH insertion, preserving
  absent versus present-undefined; Object.assign marks dynamic target writes,
  and the obsolete condAbsent side table/specialized enumerators are deleted.
  Ambiguous BOOL∪NUMBER locals correct-reject when a stored value reaches an
  identity-sensitive use; truthiness-only and captured-shadow paths remain
  accepted. dist/jz.wasm shrank 17,107.4→16,966.0 kB. Validation: conditional
  spread 21/21 (106), data 149/149 (729), number 53/53, objects 134/134,
  spread 40/41+1 skip, watr 37/37, kernel-oracle 14/14 (605), build/wat-strip
  3/3, kernel-parity 3/3, full suite 3632/3630/0/2 (21,156), functional
  self-compile 21/21 and ratchet 10/10 (+0).

## Full test:wasm loop-hoist trio — complete

- Commit 5104b467 fixes all three failures. Fresh-process reproduction
  falsified the handoff's order/state-leak diagnosis: the two perf rows failed
  only on the kernel BYTES path (compileWat printed `local.get undefined`),
  and the ablation row never hoisted. In self-compiled execution, a Map and
  later arrays captured by walkAst callbacks lost the chosen local mapping;
  spread-form `splice(...preheaders)` also failed to insert reliably. The pass
  now scans/replaces by direct recursion, keeps the tiny mapping in parallel
  arrays, and inserts one node at a time. Native decisions/order are unchanged.
  Validation: kernel perf 55/55, kernel wat-invariants 24/24, full test:wasm
  2885/2883/0/2 (13,813), build/wat-strip 3/3, native full suite
  3632/3630/0/2 (21,156), kernel-oracle 14/14, kernel-parity 3/3,
  functional self-compile 21/21 and ratchet 10/10 (+0).

## Remaining queue, in order

1. Site-by-site self-demand campaign: re-earn the −737kB — each
   hand-built-IR emitter declares its own includeFor* demand
   (buildArrayWithSpreads = the done template; object.js 'in' node,
   'str' sites, 2 stdlib-registration cases), then narrow derived-only
   CATCH_ALL autoload rows suite-gated (src/autoload.js).
2. Opaque-length redesign: custodian trail on agent/opaque-length
    (7677eb2b, NOT landable — marshalled host .length returned undefined).
    Bars: JS property access stays f64 __length/dynamic-Get; Array.from
    gets ToLength + indexed-Get; external demand at the length seam.
3. Array.from effect-order/cap gate: JS-observable evaluation order or
    reject; ARRAY_LIKE_STATIC_CAP hole.
4. Warm-pin bisect (quiet machine required; 1.04-1.06× vs 1.03 cap).
5. test262 leg (after the accepted-wrong ledger is empty).
6. FINAL: full battery (build + kernel-oracle + kernel-parity + full
    suite + goal-gate) → USER PUSHES → CI bench matrix regenerates the
    perf evidence (currently 270+ source commits stale). Push is the
    user's action, never the agent's.

## Process rules that kept this sound (binding for any resumer)

- Serial merges only; every branch validates its MERGE PRODUCT (merge
  main into branch, rebuild, battery) before merging back; bar = ZERO
  fails at the current suite total. Build before suites (stale dist =
  false fails). pwd-verify before every git/build/test command.
- Git safety: no repo-wide commands (stash/checkout/reset/clean/switch/
  restore), stage named files only, user is sole author (no
  Co-Authored-By / Generated-with), never push/pull — the user pushes.
- .work/todo-original.md in the main worktree belongs to the user.
- Agents: never park on background-wake promises (they die); hold turns
  with chained bounded-wait loops (`for i in $(seq 1 27); do <check> &&
  break; sleep 15; done`, immediately followed by the next). Zombie
  recovery that works: TaskStop, then SendMessage (resumes from
  transcript at the exact stall point). Dead-man watch: file-driven
  watchlist + transcript-mtime staleness + main-HEAD moves
  (scratchpad/deadman10.sh pattern).
- Bench/demo sources are fixed specimens: fix the engine, never the
  input. Wrong-value classes outrank performance work. KNOWN-WRONG pins
  are trail markers with named flip conditions — zero allowed at release
  on semantic paths.
