# Next isolated task

Keep `handoff-03cf346d` at `44309407` frozen. Do not amend it or clean its retained
evidence. Start a new branch/worktree from the review integration HEAD supplied
by the coordinator. Record the exact base/HEAD. Main still has eight pending
files; the clean branch excludes them. Do not copy dirty main files or label
clean-branch measurements as combined-working-tree evidence.

## 1. Close the checkpoint evidence gap

Use a private, fresh kernel build. Force the **actual internal checkpoint branch**
for small programs with a test-only source overlay or equivalent branch forcing.
Do not lower the shipping threshold or add a production benchmark knob.

Exercise the real `checkpointIR`: park, finish, rewind, unpark, then encode.
Prove that the branch ran, that records survive, and that subsequent recording
and reading still allocate zero bytes. `_clear()` alone is not this proof.
Preserve ordinary `_clear()` semantics and the diagnostic capacity/reset contract
already repaired in the integration.

Cover empty→empty, A→A, immediate A→B, fresh-B comparison, source error→A,
and alternating binary/WAT/warnings/diagnostic entries. Copy retained output
before rewind/reset; validate and execute it and reinstantiate retained bytes.
Compare forced vs normal successful output, preferably byte-for-byte. Include
an abrupt path with literal last-completed-phase reporting. Distinguish the
instrumented kernel from production bootstrap/recursive certification.

Register any new test in `test/index.js`. Run the diagnostic/summary/tape gates
and the private fresh functional self gate; list every red gate literally.

## 2. Only after that: reduce the recursive emit failure, read-only

On the same explicit clean lineage, reduce the `emitFuncs` BigInt-mixing error.
Identify the exact failing compiler function, source/AST producer, semantic kind,
physical carrier, and consuming operation. Compare native and hosted behavior;
keep the last completed phase distinct from the failing phase. Supply a minimal
ordinary-JS regression and sibling scope, error, and reuse evidence.

Do not broaden ANY→Number/BigInt assumptions, remove the Boolean guard, revive
the rejected arithmetic/member staging candidates, or delete a summary run.
No generic optimization, summary-reader change, representation/emitter repair,
A poisoning optimization, dependency/benchmark refresh, or release work is
part of this task. Ask before changing shared semantic ownership.

Use private dependencies with watr at `5ff0037`, verify file parity, and do not
write shared `node_modules`, the watr checkout, main, or generated distribution
artifacts. No merge, push, or publication. Report bytes/MiB accurately; key
characters are not cache heap bytes. Do not collect performance evidence while
other compiler jobs are running.

Return exact commits, commands, test registration, retained reproducible evidence,
missing coverage and failures. Clean only your owned expendable scratch, then
freeze the new candidate and stop awaiting review.
